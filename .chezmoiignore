# Repo documentation, not a dotfile — without this `chezmoi apply` would write
# it to ~/README.md. Everything else at the root is either a dot_/private_/
# Library source or a .chezmoi* control file, so nothing else needs listing.
README.md
